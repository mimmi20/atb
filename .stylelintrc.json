{
  "extends": ["stylelint-config-standard"],
  "plugins": [
    "stylelint-use-logical"
  ],
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["layer"]
      }
    ],
    "csstools/use-logical": ["always", { "direction": "ltr" }]
  }
}
